# mcp-audit v0.4.1

`v0.4.1` is a distribution quality patch for the adoption release.

## Highlights

- Added MIT license text to the repository and source distribution.
- Added package README metadata so PyPI displays the project description.
- Added package license metadata for downstream package managers.

## Release artifacts

The GitHub release should include the CI-built source distribution and wheel:

- `xone_mcp_audit-0.4.1.tar.gz`
- `xone_mcp_audit-0.4.1-py3-none-any.whl`

Artifacts must come from the green CI run for the release commit.

## Verification

- `python3 -m pytest tests -q`
- `git diff --check`
- `PYTHONPATH=src python3 -m mcp_audit --version`
- PyPI/TestPyPI install verification
- Homebrew formula points at this version
