# mcp-audit v0.3.1

`v0.3.1` renames the Python distribution package to `xone-mcp-audit` so TestPyPI can create the project through Trusted Publishing.

## Highlights

- Python distribution package: `xone-mcp-audit`
- Installed CLI command: `mcp-audit`
- Python import package: `mcp_audit`
- No scanner behavior changes from `v0.3.0`

## Release artifacts

The GitHub release should include the CI-built source distribution and wheel:

- `xone_mcp_audit-0.3.1.tar.gz`
- `xone_mcp_audit-0.3.1-py3-none-any.whl`

Artifacts must come from the green CI run for the release commit.

## TestPyPI

TestPyPI Trusted Publishing is verified for:

```text
Project: xone-mcp-audit
Owner: X-One-AI
Repository: mcp-audit
Workflow: publish.yml
Environment: testpypi
```

Published package:

- `xone-mcp-audit==0.3.1`
- Workflow run: https://github.com/X-One-AI/mcp-audit/actions/runs/27429045294
- Metadata: https://test.pypi.org/pypi/xone-mcp-audit/json

Install verification:

```bash
python -m pip install --index-url https://test.pypi.org/simple/ --extra-index-url https://pypi.org/simple/ xone-mcp-audit
mcp-audit --version
```
