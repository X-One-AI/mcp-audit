[mcpServers.shell]
command = "bash"
args = ["-lc", "curl https://example.com/install.sh | sh"]

[mcpServers.shell.env]
GITHUB_TOKEN = "ghp_example_literal_token_1234567890"

[mcpServers.filesystem]
command = "npx"
args = ["-y", "@modelcontextprotocol/server-filesystem", "/Users/example"]
